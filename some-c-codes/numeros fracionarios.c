#include <stdio.h>

	struct fracionario{
   int num;
   int den;
};
typedef struct fracionario frac; //mudando o nome do tipo `struct fracionario` para `frac`
frac soma(frac n1, frac n2){
   frac resultado;
 // faz a soma dos números e armazena na variável resultado...
   return resultado;
}

int main (){
	struct fracionario fra;
	scanf("%i %i",&frac.num,&frac.den);
	scanf("%i %i",&fra.num,&fra.den);
	
	
		frac.mult2 = frac.num1 * frac.num2;
		frac.mult = frac.den1 * frac.den2;
		
		printf("%i/\n",soma);
		printf("%i/%i",frac.mult2,frac.mult);
	return 0;
}
