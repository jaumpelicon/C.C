#include <stdio.h>
int main () {
	
	int a1,a2,a3,a4,a5,a6,a7,a8,a9,a10;
	float media;
	scanf("%i %i %i %i %i %i %i %i %i %i",&a1,&a2,&a3,&a4,&a5,&a6,&a7,&a8,&a9,&a10);
	  
	  media = (a1+ a2 + a3 + a4 + a5 + a6 + a7 + a8 + a9 + a10) / 10.0;
	  
	  printf ("A media eh: %.2f \n",media);
	
	
	
	
	return 0;
}
